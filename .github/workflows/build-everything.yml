name: Build Everything (JAR/XCFramework)

on:
  workflow_dispatch:

jobs:
  build-everything:
    uses: a-sit-plus/internal-workflows/.github/workflows/build-everything.yml@feature/modularTests
    with:
      matrix-file-name: ".github/config/build-strategy-matrix.json"